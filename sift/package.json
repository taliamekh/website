{
  "name": "sift",
  "version": "1.1.0",
  "description": "Skip the story, get to the recipe. Pastel pink recipe reader with cookbook, tabs, notes, and Chrome extension.",
  "type": "module",
  "private": true,
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "test": "node scripts/test-parser.js",
    "gen-icons": "node scripts/gen-icons.js",
    "setup": "npm install && npm run gen-icons"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "cheerio": "^1.0.0",
    "express": "^4.21.0",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "sharp": "^0.33.5"
  }
}
